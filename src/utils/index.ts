// 1. Calculate the total GDP for each country in the dataset
export function calcAllTimeGDP() {

}

// 2. Calcualte the average historical population for each country using their timeline
export function calcHistoricalAvgPopulation() {

}

// 3. Find the country with the highest population in a given year, ex "2022" 
export function findCountryWithHighestPopulationForYear(year: number) {

}

// 4. Find the year with the highest global/combined GDP accross all countries
// Explain using sudo-code for sake of time
export function findYearWithHighestCombinedGDP() {

}